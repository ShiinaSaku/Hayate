[package]
name        = "hayate-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "hayate_ffi"
crate-type = ["cdylib", "staticlib"]

[dependencies]
hayate-engine = { path = "../hayate-engine" }
compio        = { workspace = true }
anyhow        = { workspace = true }
